# ============================================================
# REQUIREMENTS - All Python packages needed for this project
# Install with: pip install -r requirements.txt
# ============================================================

# Groq AI library (for LLM calls)
groq>=0.4.0

# TigerGraph Python client (for graph database)
pyTigerGraph>=1.0.0

# Streamlit (for the web dashboard)
streamlit>=1.35.0

# Load environment variables from .env file
python-dotenv>=1.0.0

# For data handling
pandas>=2.0.0

# For better table display in Streamlit
tabulate>=0.9.0

# ── Dataset ───────────────────────────────────────────────────
# HuggingFace datasets (for Wikipedia knowledge base)
datasets>=2.14.0

# ── Basic RAG ────────────────────────────────────────────────
# Sentence embeddings for FAISS retrieval
sentence-transformers>=2.2.0

# Vector similarity search
faiss-cpu>=1.7.4

# ── Accuracy Evaluation ──────────────────────────────────────
# HuggingFace Hub for LLM-as-Judge
huggingface-hub>=0.20.0

# Evaluation metrics (BERTScore, etc.)
evaluate>=0.4.0

# BERTScore
bert-score>=0.3.13

# Transformers (required by evaluate / bertscore)
transformers>=4.35.0

# PyTorch CPU (required by sentence-transformers + bertscore)
torch>=2.0.0

# NumPy (required by FAISS + sentence-transformers)
numpy>=1.24.0
